/***  Use this as a template for API from future food-site team-work ***/

/*
const request = require('request-promise');

const API_KEY = 'foodApp';

class RecipeFromWww {
    static retrieveBySite (recipe, callback) {
        request({
            uri: '' ,
            json: true
        }).then(function (res) {
            callback(res);
        }).catch(function (err) {
            console.log(err);
            callback({ error: 'Could not reach ... API.'})
        });
    }
}

module.exports = RecipeFromWww;
*/